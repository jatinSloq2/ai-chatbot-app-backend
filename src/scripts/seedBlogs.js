// Run with: node src/scripts/seedBlogs.js
require("dotenv").config();
const mongoose = require("mongoose");
const connectDB = require("../config/db");
const Blog = require("../models/Blog");
const User = require("../models/User");

// All 5 posts are about JestBot itself — why it exists and what you can build
// with it (WhatsApp bot + website widget, mobile APK / embeddable SDK, tool
// calling, selling products through a bot, and AI calling agents). Cover
// images are real, valid Unsplash photo IDs. Each post runs ~1900-2200 words
// (roughly an 8-10 minute read at ~220 wpm) with several sections, an FAQ,
// and real code snippets.
const blogs = [
  {
    title: "Why We Built JestBot: One AI Bot for Chat, WhatsApp, Calls, and Everything In Between",
    slug: "why-we-built-jestbot",
    excerpt:
      "Teams end up stitching together five different tools to answer a customer's question — a website widget, a WhatsApp number, a call center script, and a spreadsheet of FAQs. JestBot exists to replace all of that with one bot.",
    content:
      "<h2>The problem we kept running into</h2><p>Every business we talked to before building JestBot had roughly the same setup, just with different vendor logos. A website chat widget from one company, handling only website visitors. A WhatsApp Business number that a support agent answered manually from their phone, with no memory of what the customer had already asked on the website. An old IVR or calling flow, built years ago by someone who has since left the company, that nobody wants to touch. And underneath all of it, a shared spreadsheet or a wiki page of FAQs that three different tools were all trying to represent in three different ways.</p><p>None of these systems talked to each other. A lead who asked about pricing on WhatsApp on Monday had to repeat the entire conversation to a human on a call on Wednesday. A customer who got a wrong answer from the website widget had no way of knowing the WhatsApp bot (if there even was one) might know better. Every channel was its own silo, with its own training data, its own dashboard, and its own bill.</p><h3>The fragmented stack anti-pattern</h3><p>If you map out a typical mid-size company's customer-facing AI stack today, it usually looks like this:</p><ul><li>A website widget vendor, trained on a static export of the help center</li><li>A separate WhatsApp Business API reseller, configured with its own keyword-based flows</li><li>A call center IVR that was never meant to understand natural language</li><li>An internal support team fielding the requests that fall through the cracks of all three</li></ul><p>Each of these tools needs to be updated separately whenever a policy changes, a product launches, or a price moves. In practice, that means they drift out of sync within weeks. The WhatsApp bot still quotes last quarter's pricing. The website widget doesn't know about the new SKU. Nobody trusts any of the automated channels, so everything eventually gets escalated to a human anyway — which defeats the point of automating in the first place.</p><h3>The real cost of disconnected channels</h3><p>It's easy to underestimate how expensive this fragmentation actually is until you add it up. Every extra tool is a separate subscription, a separate login, and a separate person who has to remember to update it. Every gap between tools is a place where a lead goes cold, because nobody followed up in time, or a customer gets a wrong answer and quietly churns instead of complaining. And every inconsistency between channels chips away at trust — a customer who catches your WhatsApp bot contradicting your website doesn't conclude \"the WhatsApp bot must be outdated,\" they conclude \"I can't trust anything this company's bots tell me,\" and start double-checking everything with a human, which erases the entire point of automating support.</p><h3>What JestBot actually is</h3><p>JestBot is a single AI bot — trained on your documents, FAQs, and product catalog — that you can deploy across a website widget, a WhatsApp number, a mobile app via SDK, and an AI calling agent, all sharing the same knowledge base, the same conversation history, and the same tools. You configure the bot's personality, instructions, and knowledge once. Every channel you turn on inherits that configuration automatically.</p><p>That single point of configuration is the whole idea. When you update a document in the bot's knowledge base, the change is live everywhere at once — the widget, WhatsApp, the mobile SDK, and the calling agent — without re-training anything or redeploying five separate systems.</p><h3>Built on Retrieval Augmented Generation</h3><p>Instead of letting the underlying language model hallucinate answers from its general training, JestBot retrieves the most relevant chunks from your uploaded documents before generating a reply, so answers stay grounded in what you actually told it. This is the same Retrieval Augmented Generation (RAG) pattern used by most production AI systems today, and it's the difference between a bot that occasionally invents a return policy and one that quotes your real one.</p><p>Under the hood, every document you upload — PDFs, Word docs, spreadsheets, or plain URLs — is split into smaller chunks, converted into vector embeddings, and stored so they can be searched by semantic similarity rather than exact keyword matches.</p><div class='code-block'><pre><code class='language-javascript'>// Simplified: how a JestBot reply is grounded in your data\nconst chunks = await ragService.retrieveRelevantChunks(botId, userMessage);\nconst reply = await llmService.generate({\n  systemPrompt: bot.instructions,\n  context: chunks,\n  userMessage,\n});</code></pre></div><p>Because the retrieval step happens on every message, the bot's answers stay current with whatever is in the knowledge base at that moment — not whatever was true when the model was last fine-tuned. If you upload a corrected pricing sheet at 10am, every channel is quoting the new price by 10:01.</p><h3>One bot, every channel</h3><p>Create the bot once in the dashboard, and turn on the channels you need without retraining anything:</p><ul><li><strong>Website Widget</strong> — a lightweight embed script for your site</li><li><strong>WhatsApp</strong> — connected through the WhatsApp Cloud API via embedded signup</li><li><strong>Mobile SDK</strong> — for a native Android or iOS app, including a packaged APK</li><li><strong>AI Calling Agent</strong> — for inbound and outbound phone conversations</li></ul><p>Every channel shares the same document set, the same tool definitions, and — where the customer is identifiable — the same conversation history. A returning customer who chatted on the website last week and calls in today doesn't have to start from zero.</p><h3>Tool calling as a first-class citizen</h3><p>Answering questions is table stakes. What actually moves the needle for a business is a bot that can take action: check real inventory, apply a discount code, create a support ticket, or book a slot on a calendar. JestBot treats tool calling as a core capability rather than an add-on, which is what turns a chatbot into an actual automation layer for the business, not just a smarter FAQ page.</p><h3>A day in the life of a connected bot</h3><p>It helps to walk through what this looks like in practice. A visitor lands on your pricing page and asks the website widget a question about your Pro plan. The bot answers from your live pricing document, and — because it's a genuine lead — logs it as one. That evening, the same person messages your WhatsApp number asking whether the plan supports a feature they need; the bot recognizes the returning lead, answers from the same knowledge base, and offers to book a demo. Two days later they call your sales line; the AI calling agent already knows they were quoted the Pro plan and asked about that specific feature, so the conversation picks up naturally instead of starting over. Nobody had to manually pass notes between channels — it was the same bot the whole time.</p><h3>Security and data handling</h3><p>Every bot's knowledge base, conversation logs, and tool credentials are scoped to your account and, where relevant, your team. Integration credentials for things like WhatsApp, payment providers, or your CRM are stored encrypted, and access to sensitive tools can be restricted per agent or per team so a support rep and an admin don't have the same blast radius if something goes wrong.</p><h3>Who this is actually for</h3><p>We built JestBot for teams who are tired of maintaining five disconnected tools to do one job: answer customers and, increasingly, close sales. That includes SaaS companies who want to reduce churn with better in-app support, coaches and creators who want to scale their expertise without hiring a team, agencies who need to support dozens of clients without linearly scaling headcount, and e-commerce brands who want a bot that can actually sell, not just answer FAQs.</p><h3>Comparing the cost of five tools versus one bot</h3><p>It's worth putting real numbers next to the fragmented-stack problem described earlier. A separate website widget vendor, a WhatsApp reseller, a calling/IVR platform, and the internal time spent keeping all three in sync each carry their own subscription cost, their own support contract, and their own onboarding curve for whoever on your team has to manage them. Beyond the subscription fees, there's a harder-to-see cost: every hour spent updating three different dashboards with the same pricing change is an hour not spent on the product or the customer relationship itself. Consolidating onto one bot doesn't just reduce the number of invoices — it reduces the number of places a mistake can happen.</p><h3>How teams actually roll this out</h3><p>The teams who get the most out of JestBot don't try to turn on every channel on day one. A typical rollout looks like this: start with the website widget, since it's the fastest channel to launch and the easiest to correct if the bot gives a wrong answer. Spend a week or two watching real conversations, refining the knowledge base and instructions based on what customers actually ask (which is almost always different from what you assumed they'd ask). Once the widget is answering confidently and correctly, connect WhatsApp through the embedded signup flow — the bot is already trained, so this step is mostly just flipping a switch. From there, tool calling gets layered in once the conversational answers are solid, and the calling agent and mobile SDK come last, once you're confident the bot's underlying knowledge and behavior are dialed in. Each step builds on the last instead of trying to get five things right simultaneously.</p><h3>Frequently asked questions</h3><p><strong>Do I need to set up each channel separately from scratch?</strong> No — you create and train one bot, and each channel (widget, WhatsApp, SDK, calling agent) is a toggle on top of that same bot, not a separate configuration.</p><p><strong>What happens if I only need one channel today?</strong> Start with just the website widget or just WhatsApp — you're not required to turn on every channel. The other channels are there when you're ready to expand, using the same bot you already trained.</p><p><strong>How current does the bot's knowledge stay?</strong> As current as your uploaded documents. Retrieval happens on every message, so there's no re-training delay between updating a document and the bot reflecting it.</p><p><strong>Is there a right order to turn channels on in?</strong> Most teams start with the website widget, move to WhatsApp once answers are reliable, and add tool calling, the calling agent, and the mobile SDK as the bot's core knowledge and behavior get more refined.</p><h3>Where this is going</h3><p>The rest of this blog covers each of these pieces in depth — the WhatsApp and widget setup, the mobile SDK and APK path, tool calling and selling through the bot, and the AI calling agent for phone conversations. If you're evaluating JestBot for the first time, the short version is this: one bot, one knowledge base, every channel your customers actually use, and the tools to let it do real work instead of just talking about it.</p>",
    coverImage: "https://images.unsplash.com/photo-1531482615713-2afd69097998?auto=format&fit=crop&w=1200&q=80",
    tags: ["JestBot", "AI Strategy", "Product"],
    published: true,
  },
  {
    title: "WhatsApp Chatbot + Website Widget: Automate Support on Every Channel with JestBot",
    slug: "whatsapp-chatbot-website-widget-jestbot",
    excerpt:
      "Most visitors want to chat on your website; most existing customers want to message you on WhatsApp. JestBot runs the same trained bot on both, so nobody falls through the cracks.",
    content:
      "<h2>Two channels, one brain</h2><p>Ask ten businesses which channel their customers prefer and you'll get two answers, split roughly down the middle. New visitors browsing your site want an instant answer without downloading anything or saving a contact — that's the website widget's job. Existing customers, especially in markets where WhatsApp is the default messaging app, want to message you the same way they message everyone else. JestBot connects to both, and — critically — both run off the exact same trained bot, so a customer gets the same quality of answer no matter which one they pick.</p><h3>The old way of doing WhatsApp support</h3><p>Before AI bots were practical, WhatsApp Business support usually meant one of two things: a human manually replying from a shared inbox app, or a rigid keyword-based flow (\"reply 1 for sales, 2 for support\") that broke the moment a customer typed a real sentence instead of a menu number. Neither scales, and neither actually understands what the customer is asking. Add a busy sales day or a product launch, and the shared inbox turns into a queue nobody can keep up with, while the keyword flow just loops the customer through the same three menu options until they give up.</p><h3>Connecting WhatsApp the right way</h3><p>JestBot connects to the WhatsApp Cloud API through an embedded signup flow, so you can link your existing WhatsApp Business number in a few clicks — no manual token juggling, no digging through Meta's Business Manager to find the right App ID. The embedded signup handles the OAuth handshake, phone number verification, and webhook subscription for you.</p><div class='code-block'><pre><code class='language-javascript'>// Simplified: kicking off WhatsApp embedded signup for a bot\nconst signupUrl = await whatsappEmbeddedSignupService.getSignupUrl({\n  botId,\n  redirectUri: \"https://app.jestbot.ai/whatsapp/callback\",\n});\n// Redirect the merchant to signupUrl to authorize their WhatsApp number</code></pre></div><p>Once authorization completes, JestBot receives a webhook confirming the phone number and business account, stores the credentials against your bot, and the number is live. There's no waiting period, no manual review queue on our side — as soon as Meta confirms the authorization, messages start flowing.</p><h3>How an inbound WhatsApp message is handled</h3><p>Every inbound message from a customer hits a webhook, gets matched to the right bot by phone number, runs through the same retrieval and tool-calling pipeline used everywhere else in JestBot, and the reply is sent back through the Cloud API.</p><div class='code-block'><pre><code class='language-javascript'>// Simplified WhatsApp webhook handler\nrouter.post(\"/whatsapp/webhook\", async (req, res) => {\n  const { from, text, botPhoneNumberId } = parseWhatsAppPayload(req.body);\n  const bot = await botService.findByPhoneNumberId(botPhoneNumberId);\n  const reply = await botService.generateReply({ botId: bot._id, channel: \"whatsapp\", from, text });\n  await whatsappSender.send(from, reply);\n  res.sendStatus(200);\n});</code></pre></div><p>Because this runs through the same bot service as every other channel, any tool the bot has access to — checking stock, creating a lead, booking a meeting — works exactly the same way on WhatsApp as it does on the website.</p><h3>Dropping the widget onto your site</h3><p>The widget is a single script tag. It loads asynchronously so it never blocks your page's initial render, and it opens a chat bubble that talks to your bot's API over a lightweight session.</p><div class='code-block'><pre><code class='language-html'>&lt;script\n  src=\"https://cdn.jestbot.ai/widget.js\"\n  data-bot-id=\"YOUR_BOT_ID\"\n  async\n&gt;&lt;/script&gt;</code></pre></div><p>The widget picks up your bot's configured name, avatar, and color scheme automatically, so it looks like part of your site rather than a bolted-on third-party tool. You can also pass optional attributes to control where the bubble sits, whether it opens automatically after a delay, and what the greeting message says.</p><h3>Customizing the widget's look and behavior</h3><p>Beyond the basic embed, the widget supports a handful of behaviors worth knowing about: a proactive greeting that opens after a configurable delay to nudge an engaged visitor into a conversation, quick-reply buttons under the bot's first message for common questions, and a position override for sites where the default bottom-right corner conflicts with another element like a cookie banner or a support button from a different tool.</p><div class='code-block'><pre><code class='language-html'>&lt;script\n  src=\"https://cdn.jestbot.ai/widget.js\"\n  data-bot-id=\"YOUR_BOT_ID\"\n  data-position=\"bottom-left\"\n  data-proactive-delay=\"15000\"\n  async\n&gt;&lt;/script&gt;</code></pre></div><h3>Shared context between channels</h3><p>When a customer is identifiable across channels — for example, they've logged in on your site and also message you on WhatsApp using a phone number tied to their account — JestBot can link those conversations so the bot has the fuller picture instead of treating every channel as a stranger. A customer who asked about a refund on the website and then follows up on WhatsApp doesn't have to explain the situation twice.</p><h3>Business hours and human handover</h3><p>Automation shouldn't mean customers are stuck talking to a bot when they clearly need a person. JestBot can detect frustration or explicit requests for a human, and route the conversation to an available agent. Outside business hours, it can queue the conversation and let the customer know when to expect a reply, on both WhatsApp and the widget.</p><div class='code-block'><pre><code class='language-javascript'>// Simplified handover trigger\nif (handoverIntent.detect(userMessage) || sentiment.isFrustrated(userMessage)) {\n  await handoverService.escalate({ conversationId, reason: \"customer_requested_human\" });\n}</code></pre></div><h3>Canned responses and consistency</h3><p>For questions that come up constantly — shipping timelines, refund windows, business hours — JestBot supports canned responses that agents can insert instantly during a handover, keeping the tone consistent between what the bot said automatically and what the human says next. This matters more than it sounds: a customer who gets a warm, on-brand automated reply and then a curt, differently-worded human reply notices the seam immediately.</p><h3>Analytics across both channels</h3><p>Because WhatsApp and the widget both flow through the same bot, your analytics dashboard shows combined numbers: total conversations, messages per month against your plan limit, common questions, and handover rate — broken down by channel so you can see whether WhatsApp or the widget is doing more of the work, and where each channel's customers tend to need a human.</p><h3>Common pitfalls to avoid</h3><ul><li>Uploading outdated documents and expecting the bot to know the difference — the bot only knows what's in the knowledge base right now</li><li>Turning on WhatsApp before testing the bot thoroughly on the widget, where mistakes are cheaper to catch</li><li>Not setting business hours, so customers get an automated reply at 2am when they actually needed a human follow-up in the morning</li><li>Forgetting to add tags and canned responses that make human handover fast when it does happen</li><li>Leaving the widget's proactive greeting too aggressive, which can feel intrusive on a first-time visitor</li></ul><h3>Notifications and message templates on WhatsApp</h3><p>WhatsApp's platform draws a distinction between a reply inside an open conversation window and a message you initiate outside that window — the latter requires a pre-approved message template, which is a Meta requirement rather than a JestBot one. JestBot handles submitting and managing these templates for you, so when your bot needs to proactively reach out — an order status update, an abandoned-cart nudge, an appointment reminder — it can send a properly formatted template message instead of the conversation silently failing because the customer hasn't messaged recently. Understanding this distinction matters if you plan to use WhatsApp for anything beyond reactive support, since template approval can take a day or two the first time you submit one.</p><h3>Multiple bots, multiple numbers</h3><p>Larger businesses, or agencies managing several client accounts, often need more than one WhatsApp number or more than one widget — for different brands, different regions, or different clients entirely. JestBot supports running multiple bots under one account, each with its own knowledge base, WhatsApp number, and widget, so a single dashboard can manage several distinct customer-facing bots without any of their knowledge or conversation history bleeding into each other.</p><h3>Language support and localization</h3><p>Customers messaging on WhatsApp often write in a mix of languages within the same conversation, switching between English and a regional language mid-sentence — something rigid keyword bots handle badly. Because JestBot's underlying model understands natural language rather than matching fixed phrases, it can follow that kind of code-switching and reply in whichever language the customer is using, drawing on the same knowledge base regardless of the language the conversation is happening in. The website widget supports the same behavior, so a visitor typing in a regional language gets a coherent reply instead of a generic \"please use English\" message.</p><h3>A quick before-and-after</h3><p>Picture a small clothing brand before JestBot: a website contact form nobody checks until the next morning, a WhatsApp number one person answers between other tasks, and a habit of losing track of who already asked about a specific order. After connecting both channels to one bot: a visitor asks about a size on the website at 11pm and gets an instant, accurate answer from the actual size chart document instead of a form that goes nowhere. A regular customer messages on WhatsApp the next day asking to track that same order, and because the bot recognizes the returning customer, it can jump straight to the tracking answer instead of re-asking for the order number. The person who used to spend their afternoon answering the same five questions over and over is now only pulled in for the handful of conversations that actually need a human — refund edge cases, custom requests, escalations.</p><h3>Frequently asked questions</h3><p><strong>Can I use my existing WhatsApp Business number?</strong> Yes — the embedded signup flow links your existing number rather than requiring a new one.</p><p><strong>Will the widget slow down my website?</strong> The script loads asynchronously and doesn't block your page's initial render, so it shouldn't affect load-time metrics in any meaningful way.</p><p><strong>Do WhatsApp and the widget share the same conversation history for a customer?</strong> When a customer is identifiable on both channels (for example, a logged-in user whose phone number matches their WhatsApp), yes — otherwise each channel starts its own conversation thread.</p><p><strong>Does the bot handle customers switching languages mid-conversation?</strong> Yes — because it understands natural language rather than matching fixed keywords, it can follow a conversation that mixes languages and reply appropriately.</p><h3>Getting started</h3><p>Set up the widget first, since it's the fastest way to validate the bot's answers with real traffic. Once you're happy with its accuracy, connect WhatsApp through the embedded signup flow and let the same bot pick up conversations there — no extra training, no separate configuration, just a second channel for the same brain. Watch the first few dozen real conversations closely on both channels, correct anything the bot gets wrong by updating the knowledge base rather than manually overriding individual replies, and only then start layering on tool calling, the calling agent, or the mobile SDK once you trust the foundation these two channels are built on.</p>",
    coverImage: "https://images.unsplash.com/photo-1611926653458-09294b3142bf?auto=format&fit=crop&w=1200&q=80",
    tags: ["WhatsApp", "Chat Widget", "Automation"],
    published: true,
  },
  {
    title: "From Website Widget to Mobile APK: Embedding JestBot Everywhere with the SDK",
    slug: "jestbot-mobile-apk-sdk",
    excerpt:
      "A chatbot that only lives on your website misses everyone using your mobile app. JestBot's SDK lets you embed the same bot inside an Android APK, iOS app, or any custom product with a few lines of code.",
    content:
      "<h2>Why a web-only bot isn't enough</h2><p>A huge share of usage for consumer and B2B products alike happens inside a native mobile app, not a mobile browser. If your AI support bot only lives on your website, you're covering the smaller half of your traffic. A website widget is great for browsers, but it fundamentally can't be embedded inside a compiled Android APK or an iOS app — those are different runtimes with different rules. That's exactly the gap JestBot's SDK is built to close.</p><h3>What the SDK actually gives you</h3><p>JestBot's SDK exposes the same chat, streaming, and tool-calling capabilities that power the website widget and WhatsApp, as a set of REST and WebSocket calls you can wire into your own native chat screen. Instead of an iframe or a webview hack, you get a real client library that fits naturally into a React Native, Kotlin, or Swift codebase, and behaves like any other typed dependency in your project rather than an embedded web page pretending to be native.</p><h3>Installing and initializing the SDK</h3><div class='code-block'><pre><code class='language-bash'>npm install @jestbot/sdk</code></pre></div><div class='code-block'><pre><code class='language-javascript'>import { JestBotClient } from \"@jestbot/sdk\";\n\nconst client = new JestBotClient({\n  botId: \"YOUR_BOT_ID\",\n  apiKey: process.env.JESTBOT_PUBLIC_KEY,\n});\n\nconst session = await client.createSession({ userId: currentUser.id });</code></pre></div><p>The public API key is scoped for client-side use — it can start sessions and send messages, but it can't read or modify your bot's configuration, so it's safe to ship inside a compiled app without exposing anything sensitive if someone decompiles your APK.</p><h3>Streaming a reply into your Android app</h3><p>Just like the website widget, the SDK streams tokens as they're generated instead of waiting for the full response, so your chat UI feels responsive even on a slower mobile connection.</p><div class='code-block'><pre><code class='language-javascript'>client.sendMessage(session.id, \"Do you deliver to Jaipur?\", {\n  onToken: (token) => appendToChatBubble(token),\n  onComplete: (fullReply) => saveToLocalHistory(fullReply),\n  onError: (err) => showRetryOption(err),\n});</code></pre></div><h3>A native iOS integration looks just as simple</h3><p>The same session and streaming model applies on iOS through the Swift package, so a native Swift app gets the same experience as a React Native or Android build, just with the idioms of that platform.</p><div class='code-block'><pre><code class='language-swift'>let client = JestBotClient(botId: \"YOUR_BOT_ID\", apiKey: publicKey)\nlet session = try await client.createSession(userId: currentUser.id)\n\ntry await client.sendMessage(sessionId: session.id, text: userInput) { token in\n    chatViewModel.appendToken(token)\n}</code></pre></div><h3>Persisting sessions across app restarts</h3><p>Mobile users close and reopen apps constantly, and a chat history that resets every time feels broken. The SDK lets you persist the session ID locally and resume the same conversation, including the bot's memory of what was already discussed, the next time the user opens the app.</p><div class='code-block'><pre><code class='language-javascript'>// On app launch\nconst savedSessionId = await AsyncStorage.getItem(\"jestbot_session_id\");\nconst session = savedSessionId\n  ? await client.resumeSession(savedSessionId)\n  : await client.createSession({ userId: currentUser.id });\n\nawait AsyncStorage.setItem(\"jestbot_session_id\", session.id);</code></pre></div><h3>Building a native chat screen</h3><p>Because the SDK just returns plain text (or streamed tokens) and structured tool results, you're free to design the chat UI however fits your app's design system — bubbles, cards, quick-reply buttons — rather than being stuck with an embedded widget's fixed look.</p><div class='code-block'><pre><code class='language-javascript'>function ChatScreen() {\n  const [messages, setMessages] = useState([]);\n\n  const handleSend = async (text) => {\n    setMessages((m) => [...m, { role: \"user\", text }]);\n    let botText = \"\";\n    await client.sendMessage(session.id, text, {\n      onToken: (t) => { botText += t; setMessages((m) => [...m.slice(0, -1), { role: \"bot\", text: botText }]); },\n    });\n  };\n\n  return &lt;ChatList messages={messages} onSend={handleSend} /&gt;;\n}</code></pre></div><h3>Handling offline and flaky connections gracefully</h3><p>Mobile networks drop far more often than a home wifi connection, so the SDK is built to queue an outgoing message if the connection briefly drops and retry automatically, rather than silently failing and leaving the customer wondering whether their message ever sent. Your UI can hook into a connection-state callback to show a subtle \"reconnecting…\" indicator rather than a hard error.</p><div class='code-block'><pre><code class='language-javascript'>client.on(\"connectionStateChange\", (state) => {\n  if (state === \"reconnecting\") showBanner(\"Reconnecting…\");\n  if (state === \"connected\") hideBanner();\n});</code></pre></div><h3>Push notifications tie-in</h3><p>When a bot conversation is handed over to a human agent, or when the bot needs to follow up after a tool call finishes (like an order status changing), the SDK can trigger a push notification through your existing notification service, so the customer doesn't have to keep the app open and staring at the screen to get an answer.</p><h3>Packaging into an APK</h3><p>None of this is JestBot-specific packaging — you build your Android app the normal way, with the SDK as a dependency, and generate your APK (or app bundle) through your usual Gradle build. The SDK adds no unusual native dependencies beyond standard networking and WebSocket libraries, so it doesn't bloat your build or introduce unusual permissions requirements that might slow down your Play Store review.</p><div class='code-block'><pre><code class='language-bash'>./gradlew assembleRelease\n# outputs app/build/outputs/apk/release/app-release.apk</code></pre></div><h3>Debugging and testing before you ship</h3><p>The SDK exposes a debug mode that logs every request, tool call, and streamed token to your console during development, so you can see exactly what the bot decided to do and why before you build a release APK. It's worth leaving this on through QA and switching it off for your production build.</p><div class='code-block'><pre><code class='language-javascript'>const client = new JestBotClient({ botId, apiKey, debug: __DEV__ });</code></pre></div><h3>One bot, one config, every surface</h3><p>The part that matters most: because the SDK talks to the same bot configuration as the website widget and WhatsApp number, an update to your knowledge base, your instructions, or your available tools instantly applies everywhere — web, WhatsApp, and your packaged mobile APK — without shipping a new app version or retraining anything.</p><h3>When to reach for the SDK vs. the widget</h3><ul><li>Use the <strong>website widget</strong> when you want a fast, zero-code embed for a marketing site, docs site, or web app</li><li>Use the <strong>SDK</strong> when you're building or already have a native mobile app and want the bot's UI to match your app's design language exactly</li><li>Use <strong>both</strong> when your product spans web and mobile — they share the same bot, so there's no duplicate configuration either way</li></ul><h3>Tool calling and calls from inside the app</h3><p>Every capability covered elsewhere in this series works through the SDK exactly the way it works through the widget or WhatsApp. A customer inside your app can ask the bot to check whether an item is in stock, and the same tool orchestrator that powers the website widget runs the check and returns a real answer, not a canned response. If your app also has calling built in, a tap-to-call button can route into the same AI calling agent, so a customer who starts in the app and wants to talk it through by voice doesn't have to leave your product and dial a separate number — the handoff from in-app chat to a live or AI-assisted call can happen inside the same session.</p><h3>Versioning and backward compatibility</h3><p>Mobile apps update far more slowly than websites — a customer might keep an old version of your APK installed for months after a new one ships, especially if auto-update is disabled or they're on a slow connection. The SDK is versioned and backward compatible across bot configuration changes, meaning you can update your bot's knowledge base, instructions, or tools on the server side without needing every user to update their app first. The one exception is if you add a genuinely new SDK feature (like a new message type) — that does require a newer SDK version in the app itself, the same as any other client library.</p><h3>Security considerations when shipping a compiled app</h3><p>A compiled Android APK can be decompiled by anyone with a little patience, which is exactly why the SDK's public API key is deliberately limited in what it can do — session creation and messaging only, with no access to your bot's configuration, documents, or private tool credentials. If you're adding custom tools that touch sensitive systems, keep the actual credentials for those systems on your own backend behind the webhook, rather than passing anything sensitive through the client. The SDK's job is to get a message from the app to the bot and a reply back, not to hold secrets.</p><h3>Analytics from inside your app</h3><p>Conversations started through the SDK show up in the same analytics dashboard as the widget and WhatsApp, tagged by channel, so you can see how much support volume is coming from inside your app versus your website. This is often more revealing than teams expect — a product with a strong native app frequently finds that in-app users ask noticeably different questions than website visitors, since they're usually already customers rather than people evaluating whether to sign up, and the questions skew toward \"how do I do X\" rather than \"what does this cost.\"</p><h3>Frequently asked questions</h3><p><strong>Does the SDK work with React Native, or only native Android/iOS?</strong> All three — the JavaScript SDK works in React Native directly, and native Swift/Kotlin packages are available for fully native apps.</p><p><strong>Is the public API key safe to ship inside a compiled app?</strong> Yes — it's scoped for client-side session creation and messaging only, and can't read or change your bot's configuration or knowledge base.</p><p><strong>Do I need a backend of my own to use the SDK?</strong> No — the SDK talks directly to JestBot's API. A backend of your own only becomes necessary if you're adding custom tools that call into your own systems.</p><p><strong>Can I see whether my mobile app or my website drives more bot conversations?</strong> Yes — the analytics dashboard breaks down conversations by channel, including the SDK, so you can compare volume and topics across your app and your website.</p><h3>What's next</h3><p>Once your bot is live inside your app, the same tool-calling and calling-agent capabilities covered elsewhere in this series apply here too — a mobile user can check order status, get product recommendations, or escalate to a human, all inside your native app rather than being bounced out to a browser or a phone call. Because the SDK, the widget, WhatsApp, and the calling agent all sit on top of the exact same bot, none of this requires a second integration effort — you're extending the reach of the bot you've already built, not starting a new project for every surface your customers happen to use.</p>",
    coverImage: "https://images.unsplash.com/photo-1512941937669-90a1b58e7e9c?auto=format&fit=crop&w=1200&q=80",
    tags: ["SDK", "Mobile", "APK", "Integration"],
    published: true,
  },
  {
    title: "Tool Calling in JestBot: How Your AI Bot Actually Sells Products and Takes Action",
    slug: "jestbot-tool-calling-sell-products",
    excerpt:
      "A chatbot that can only answer questions is a search bar with extra steps. Tool calling is what lets a JestBot agent check stock, create an order, and actually sell your product mid-conversation.",
    content:
      "<h2>Answering vs. doing</h2><p>Most chatbots stop at answering FAQs. Ask them about a product's return policy and they'll do fine. Ask them to actually check whether a specific size is in stock right now, apply a discount code, and place the order — and they fall apart, because they were never given a way to reach outside the conversation and touch real systems. JestBot supports tool calling, which is what closes that gap: the bot can call real functions against your inventory system, your CRM, your calendar, or your payment provider, instead of just describing what it would theoretically do.</p><h3>How tool calling works under the hood</h3><p>Modern language models can be given a list of available \"tools\" — essentially function signatures with a name, a description, and a set of typed parameters. When the model decides a tool is needed to answer accurately, it doesn't guess an answer; it emits a structured call to that tool instead. Your backend executes the actual function, returns the result to the model, and the model folds that real data into its final reply to the customer. This loop can repeat several times in a single turn if the model needs more than one piece of live data to answer properly.</p><h3>Defining a tool</h3><p>A tool definition in JestBot looks like a small JSON schema describing what the function does and what arguments it needs.</p><div class='code-block'><pre><code class='language-javascript'>const checkStockTool = {\n  name: \"check_product_stock\",\n  description: \"Checks live stock for a product SKU and size\",\n  parameters: {\n    type: \"object\",\n    properties: {\n      sku: { type: \"string\" },\n      size: { type: \"string\" },\n    },\n    required: [\"sku\"],\n  },\n};</code></pre></div><p>The description matters more than it looks — it's what the model reads to decide when this tool is relevant. A vague description leads to a bot that either never calls the tool when it should, or calls it when it shouldn't. Writing a clear, specific description is the single highest-leverage thing you can do when defining a new tool.</p><h3>Letting the model decide when to call it</h3><p>You don't hard-code \"if the user mentions stock, call this function.\" Instead, you hand the model the full list of available tools for the conversation, and it decides — based on what the customer actually asked — whether a tool call is needed at all.</p><div class='code-block'><pre><code class='language-javascript'>const result = await toolOrchestrator.run({\n  tools: [checkStockTool, applyCouponTool, createOrderTool],\n  onToolCall: async (call) => {\n    if (call.name === \"check_product_stock\") return inventoryService.getStock(call.args.sku, call.args.size);\n    if (call.name === \"apply_coupon\") return couponService.validateAndApply(call.args.code, call.args.orderId);\n    if (call.name === \"create_order\") return orderService.create(call.args);\n  },\n});</code></pre></div><p>If the customer just asks \"what's your return policy?\", no tool gets called at all — the answer comes straight from the retrieved documentation, the same as any other RAG-grounded reply. Tools only fire when the question genuinely needs live data or a real action.</p><h3>Chaining multiple tools into a real sale</h3><p>The real value shows up when tools chain together. A single customer message like \"I want the medium in blue, and I have a discount code SAVE10\" can trigger a sequence: check stock for that SKU and size, validate the coupon, create the order, and send a confirmation — all inside one conversational turn.</p><div class='code-block'><pre><code class='language-javascript'>// Simplified multi-step flow inside one conversation\nconst stock = await inventoryService.getStock(sku, \"M\");\nif (stock.available > 0) {\n  const coupon = await couponService.validateAndApply(\"SAVE10\", null);\n  const order = await orderService.create({ sku, size: \"M\", couponId: coupon.id, customerId });\n  await whatsappSender.send(customerPhone, `Order confirmed! ${order.id} — total ₹${order.total}`);\n}</code></pre></div><p>From the customer's point of view, they just had one conversation and walked away with a confirmed order — on the website widget, on WhatsApp, or over a phone call with the AI calling agent, since the same tool orchestrator runs behind every channel.</p><h3>Beyond checkout: lead capture and scheduling tools</h3><p>Not every business sells a physical product through a bot — plenty use the exact same tool-calling mechanism to qualify and capture leads, or to book a call instead of closing a sale directly. A <code>create_lead</code> tool can push a qualified conversation straight into your CRM with the details the bot already gathered, and a <code>schedule_demo</code> tool can check a sales rep's real calendar availability and lock in a slot without anyone touching a scheduling link.</p><div class='code-block'><pre><code class='language-javascript'>const scheduleDemoTool = {\n  name: \"schedule_demo\",\n  description: \"Books a product demo on the sales team's shared calendar\",\n  parameters: {\n    type: \"object\",\n    properties: { date: { type: \"string\" }, time: { type: \"string\" }, email: { type: \"string\" } },\n    required: [\"date\", \"time\", \"email\"],\n  },\n};</code></pre></div><h3>Guardrails: not every action should be fully automatic</h3><p>Not every tool should execute without a check. For higher-risk actions — issuing a refund above a certain amount, canceling a subscription, or making changes to account-level settings — JestBot supports requiring human approval before the tool actually runs, so the bot can prepare the action and hand it to an agent to confirm rather than executing it blind.</p><div class='code-block'><pre><code class='language-javascript'>const refundTool = {\n  name: \"issue_refund\",\n  description: \"Issues a refund for an order\",\n  requiresApproval: (args) => args.amount > 2000,\n  parameters: { type: \"object\", properties: { orderId: { type: \"string\" }, amount: { type: \"number\" } } },\n};</code></pre></div><h3>Handling tool failures gracefully</h3><p>Real systems fail — an inventory API times out, a payment provider is briefly down, a webhook returns an error. A tool call that fails shouldn't crash the conversation or leave the customer staring at a blank reply. JestBot's tool orchestrator catches failures and lets the model explain the situation honestly and offer a fallback, such as taking the order manually or trying again in a moment, instead of pretending the action succeeded.</p><div class='code-block'><pre><code class='language-javascript'>try {\n  return await inventoryService.getStock(call.args.sku);\n} catch (err) {\n  return { error: true, message: \"Stock system is temporarily unavailable\" };\n}</code></pre></div><h3>Extending with custom tools and webhooks</h3><p>Your inventory system, CRM, and payment provider are all different, so JestBot doesn't lock you into a fixed set of tools. Any tool can be backed by a webhook to your own backend, meaning you can connect a bot to essentially any internal system that exposes an API — a booking system, a ticketing platform, a Google Sheet, or a custom microservice.</p><div class='code-block'><pre><code class='language-javascript'>const bookMeetingTool = {\n  name: \"book_meeting\",\n  description: \"Books a meeting slot on the sales team's calendar\",\n  webhookUrl: \"https://yourapp.com/api/webhooks/book-meeting\",\n  parameters: { type: \"object\", properties: { date: { type: \"string\" }, time: { type: \"string\" } } },\n};</code></pre></div><h3>Real-world example: the full checkout flow</h3><p>Put together, a typical selling conversation looks like this: the bot answers a product question from the knowledge base, checks live stock when the customer asks about a specific variant, applies a coupon if one is mentioned, creates the order once the customer confirms, and sends a receipt — either through WhatsApp, the website widget, or read aloud on a call. None of this requires the customer to leave the conversation and go find a checkout page, which is exactly why tool-calling bots convert better than FAQ-only ones.</p><h3>Measuring the impact</h3><p>Because every tool call is logged against the conversation, your analytics dashboard can show not just \"messages sent\" but outcomes: how many conversations resulted in a completed order, how often a coupon tool was triggered, and where in the funnel customers drop off before a sale completes — data a plain FAQ bot simply can't give you.</p><h3>Tools that read vs. tools that write</h3><p>It helps to mentally split your tools into two categories. Read tools — checking stock, looking up an order status, fetching a customer's plan details — carry very little risk if the bot calls them a bit too eagerly, since they don't change anything. Write tools — creating an order, applying a coupon, issuing a refund, canceling a subscription — change real state and deserve more scrutiny, tighter parameter validation, and in many cases the approval step described above. A useful rule when designing a new tool is to ask what the worst case looks like if the model calls it with slightly wrong arguments; for a read tool the answer is usually \"a slightly odd reply,\" while for a write tool it can be \"a real order nobody wanted.\" Size your guardrails accordingly.</p><h3>Selling across channels with the same tools</h3><p>A tool defined once works identically whether the customer is on the website widget, messaging on WhatsApp, chatting inside your mobile app through the SDK, or talking to the AI calling agent on the phone. That means a discount code you're running as a WhatsApp-only campaign this week can be extended to every channel by simply mentioning it in the bot's instructions — no separate integration work for each surface. This is also what makes cross-channel promotions practical: a customer who hears about a sale through an outbound reminder call can complete the purchase over WhatsApp minutes later, with the same coupon tool validating the same code either way.</p><h3>Testing tools before they go live</h3><p>A tool that's wrong in production doesn't just give a bad answer — it can create a real order, apply a coupon that shouldn't exist, or issue a refund nobody approved. Before turning a new tool on for real customers, it's worth running it against a sandbox version of your inventory, CRM, or payment system, or at minimum a set of test SKUs and test coupon codes, and deliberately trying to break it: ask for an out-of-stock item, apply an expired coupon, request a quantity larger than what's available. A tool that fails cleanly and lets the model explain the situation is ready; a tool that throws an unhandled error or silently does the wrong thing needs another pass.</p><h3>Rate limiting and abuse prevention</h3><p>Once a bot can take real actions like creating orders or applying coupons, it becomes a target the same way any checkout flow is — someone might try to spam a coupon-application tool to find a working code, or flood an order-creation tool as a denial-of-service attempt. JestBot rate-limits tool calls per conversation and per customer, and higher-risk tools like coupon redemption and refunds can be configured with their own tighter limits, so a single conversation can't hammer a sensitive action beyond what a normal customer interaction would ever need.</p><h3>Frequently asked questions</h3><p><strong>Can a tool call real payment providers, not just check inventory?</strong> Yes — any external API your business already uses, including payment providers, can be wired up as a tool through a webhook.</p><p><strong>What stops the bot from taking a risky action by mistake?</strong> The <code>requiresApproval</code> option lets you force human confirmation on any tool above a threshold you define, so higher-risk actions never execute unattended.</p><p><strong>Do tools work the same on every channel?</strong> Yes — the tool orchestrator is shared across the widget, WhatsApp, the mobile SDK, and the AI calling agent, so a tool you define once works identically everywhere.</p><p><strong>Is there protection against someone spamming a tool like coupon redemption?</strong> Yes — tool calls are rate-limited per conversation and per customer, with tighter limits available for sensitive actions.</p><h3>Where to start</h3><p>Start with one high-value tool — usually stock checking or order creation — get it working reliably on the widget, then extend to a second tool like coupon application. Once a small chain of tools is solid, the same configuration works identically on WhatsApp, the mobile SDK, and the AI calling agent, since they all run through the same tool orchestrator.</p>",
    coverImage: "https://images.unsplash.com/photo-1556740758-90de374c12ad?auto=format&fit=crop&w=1200&q=80",
    tags: ["Tool Calling", "E-commerce", "Automation"],
    published: true,
  },
  {
    title: "AI Calling Agents: How JestBot Automates Real Phone Conversations",
    slug: "jestbot-ai-calling-agent",
    excerpt:
      "Chat and WhatsApp don't cover every customer — some people still want to talk to someone on the phone. JestBot's AI calling agent picks up, understands intent, and can transfer to a human when it matters.",
    content:
      "<h2>Why calling still matters</h2><p>It's tempting to assume every customer interaction has moved to chat, but for a lot of businesses — clinics, real estate, agencies, home services, delivery — the phone is still the primary channel, especially for anyone less comfortable typing out a detailed request, or anyone calling from a car or a job site where typing isn't practical. Ignoring that channel means ignoring a real slice of your customers. JestBot's AI calling agent extends the same trained bot to real phone calls: answering FAQs, qualifying leads, and booking appointments over voice, without needing a human on every call.</p><h3>How the pipeline works</h3><p>An AI calling agent is, at a high level, three systems chained together in real time: speech-to-text to turn the caller's voice into words, the same language model and tool-calling pipeline used on chat and WhatsApp to decide what to say, and text-to-speech to turn that reply back into audio the caller hears. The trick isn't any one of those three pieces — each is fairly standard technology on its own — it's making the whole chain fast enough that the conversation feels natural instead of laggy.</p><h3>Configuring a calling agent</h3><p>Setting up a calling agent starts the same way as any other channel: point it at an existing bot, so it inherits the same knowledge base, instructions, and tools. Then you configure the phone number, greeting, and fallback behavior.</p><div class='code-block'><pre><code class='language-javascript'>const agent = await agentService.createCallingAgent({\n  botId,\n  phoneNumber: \"+91XXXXXXXXXX\",\n  greeting: \"Hi, thanks for calling — how can I help you today?\",\n  fallbackToHuman: true,\n  recordCalls: true,\n});</code></pre></div><h3>Turning speech into action</h3><p>Incoming audio is transcribed in real time, routed through the same bot and tool-calling pipeline used on chat and WhatsApp, and the reply is turned back into speech and streamed to the caller — all while the call is still live.</p><div class='code-block'><pre><code class='language-javascript'>onTranscript(async (text, callId) => {\n  const reply = await botService.generateReply({ botId, channel: \"call\", text });\n  if (reply.needsHuman) return handoverService.transferCall(callId, \"support-team\");\n  await voiceService.speak(callId, reply.text);\n});</code></pre></div><p>Because this runs through the same tool orchestrator used for chat and WhatsApp, a caller can do things like check an order status, book an appointment, or hear live product availability read back to them, exactly the way a chat customer could type the same request and get a tool-backed answer.</p><h3>Latency: the make-or-break factor for voice</h3><p>In a text conversation, a two-second delay before a reply barely registers. In a phone call, two seconds of silence feels like the line dropped. Getting an AI calling agent to feel natural means minimizing round-trip latency at every stage — streaming the transcription as the caller speaks rather than waiting for them to finish, starting to generate a reply as soon as enough context is available, and streaming the synthesized speech back rather than waiting for the full reply to be generated first.</p><h3>Handling interruptions and barge-in</h3><p>Real conversations aren't strictly turn-based — people interrupt, correct themselves mid-sentence, or start talking while the agent is still speaking. A calling agent that can't handle that feels robotic and frustrating. JestBot's calling agent supports barge-in, meaning if the caller starts speaking while the bot is mid-reply, the bot stops talking and listens, the same way a human would naturally yield the floor.</p><h3>Outbound calls, not just inbound</h3><p>The calling agent isn't limited to picking up incoming calls — it can also place outbound calls for use cases like appointment reminders, abandoned-cart follow-ups, or re-engaging a lead who went quiet after a demo request. The same knowledge base and tools apply, so an outbound reminder call can also handle a reschedule request from the customer on the spot instead of requiring a callback.</p><div class='code-block'><pre><code class='language-javascript'>await agentService.placeOutboundCall({\n  botId,\n  to: leadPhoneNumber,\n  purpose: \"appointment_reminder\",\n  context: { appointmentTime: \"2026-09-02T10:00:00Z\" },\n});</code></pre></div><h3>Multi-language support on calls</h3><p>Because speech-to-text and text-to-speech both support multiple languages, the calling agent can detect the language a caller is speaking in and respond in kind, drawing on the same underlying knowledge base regardless of which language the conversation happens in. This matters especially for calling agents serving a broad regional customer base where callers may switch between a local language and English mid-call.</p><h3>Escalating to a human when it matters</h3><p>Not every call should stay fully automated. If the bot detects the caller is frustrated, explicitly asks for a person, or is dealing with something outside what the bot's knowledge base and tools can handle, it transfers the call to a live agent — ideally with a quick summary of what's already been discussed, so the caller doesn't have to repeat themselves from scratch.</p><div class='code-block'><pre><code class='language-javascript'>await handoverService.transferCall(callId, \"support-team\", {\n  summary: conversationSummary,\n  reason: \"customer_requested_human\",\n});</code></pre></div><h3>Compliance and disclosure</h3><p>Voice AI comes with obligations that text-based bots don't have to worry about as much — many jurisdictions require disclosing that a caller is speaking with an automated system, and call recording often has its own consent requirements. JestBot's calling agent can be configured to open every call with a disclosure and to respect regional recording-consent rules, so automating your phone line doesn't mean cutting corners on compliance.</p><h3>Analytics on your calling agent</h3><p>Every call is logged the same way a chat or WhatsApp conversation is — transcripts, tool calls made during the call, and whether it was escalated to a human — so you can see call volume, average handling time, and escalation rate on the same dashboard you already use for the other channels, rather than needing a separate call-center analytics tool.</p><h3>Use cases where this shines</h3><ul><li><strong>Appointment booking</strong> — a clinic or salon's calling agent can check real availability and book a slot without a receptionist picking up every call</li><li><strong>Lead qualification</strong> — a sales team's agent can ask qualifying questions and only route hot leads to a human rep</li><li><strong>Order status and support</strong> — an e-commerce brand's agent can look up an order by phone number and read back the current status</li><li><strong>After-hours coverage</strong> — calls outside business hours get handled instead of going to voicemail and being forgotten</li><li><strong>Proactive reminders</strong> — outbound calls for appointment confirmations or re-engaging a lead that's gone cold</li></ul><h3>What a caller actually experiences</h3><p>Walk through a real call: the phone rings, the agent picks up and delivers its greeting along with the required AI disclosure, and the caller explains what they need in their own words, no menu tree required. If they ask about appointment availability, the agent checks the real calendar through the same tool-calling pipeline covered elsewhere in this series, reads back two or three open slots, and books whichever one the caller picks, confirming the date and time out loud before hanging up. If partway through the caller says something like \"actually, let me talk to someone,\" the agent doesn't argue or try to redirect them back into the automated flow — it transfers the call immediately, along with a summary of what's already been covered, so the human picking up isn't starting cold.</p><h3>Rolling out a calling agent without disrupting your team</h3><p>Teams sometimes worry that introducing a calling agent will confuse existing staff or make callers feel like they've reached the wrong number. In practice, the safest rollout keeps the human team fully in the loop: start with the calling agent handling only after-hours calls, where there's no existing human process being replaced, then gradually expand its hours as your team gets comfortable seeing which calls it handles well and which ones it correctly escalates. This also gives you a real, low-risk sample of transcripts to review before trusting it with your busiest hours.</p><h3>Same brain, one more channel</h3><p>The point that ties this whole series together: because the calling agent shares the bot's knowledge base and tools with the website widget, WhatsApp number, and mobile SDK, a lead who calls in gets the exact same answers — and the same ability to check stock, apply a coupon, or book a slot — as one who messages first. Update the bot's knowledge base once, and every channel, including the phone, reflects it immediately.</p><h3>Integrating with your existing phone number</h3><p>Most businesses don't want a brand-new phone number added to their marketing materials just to run an AI agent — they want their existing landline or business number to be the one that gets smarter. JestBot's calling agent can be configured to sit behind your existing number through call forwarding or SIP trunking, so customers keep dialing the number they already know while the agent handles the answering, and calls that need a human are forwarded onward to your existing team's line rather than requiring you to publish a second number.</p><h3>Training the agent specifically for voice</h3><p>A knowledge base written for a chat bot doesn't always translate perfectly to voice. Written FAQs are often dense with bullet points, links, and formatting that make sense on a screen but sound clumsy read aloud. It's worth reviewing your bot's instructions with voice in mind — favoring shorter sentences, avoiding long lists that are hard to follow by ear, and spelling out anything a caller would need to write down, like an order number or a confirmation code, more slowly and clearly than you would type it in a chat reply. The underlying knowledge base stays the same across channels, but a calling agent's instructions can be tuned separately for how it should phrase things out loud.</p><h3>Frequently asked questions</h3><p><strong>Can the calling agent make outbound calls, or only answer inbound ones?</strong> Both — it can pick up inbound calls and place outbound calls for reminders, confirmations, or re-engagement.</p><p><strong>What happens if the bot doesn't understand what the caller said?</strong> It asks a clarifying question, the same way it would in chat, rather than guessing — and if it's still unclear after a follow-up, it can hand the call to a human.</p><p><strong>Does every call get recorded?</strong> Recording is configurable per calling agent and follows your region's consent requirements, so you control whether and how calls are recorded.</p><p><strong>Do I need a new phone number for the calling agent?</strong> No — it can sit behind your existing number through call forwarding or SIP trunking, so customers keep calling the number they already know.</p><h3>Getting started with a calling agent</h3><p>Start by validating your bot's answers thoroughly on the website widget or WhatsApp first, where mistakes are easier to catch and cheaper to fix. Once you trust the bot's accuracy and its tool calls, turn on the calling agent for a single phone number, keep human fallback switched on, and expand from there as you see how real callers actually talk to it — voice conversations tend to be shorter and more direct than typed ones, so expect to tune the bot's instructions slightly for that difference.</p>",
    coverImage: "https://images.unsplash.com/photo-1553877522-43269d4ea984?auto=format&fit=crop&w=1200&q=80",
    tags: ["AI Calling Agent", "Voice AI", "Automation"],
    published: true,
  },
];

const run = async () => {
  await connectDB();

  let admin = await User.findOne({ role: "admin" });
  if (!admin) {
    console.log("No admin user found. Creating one for blog authorship...");
    admin = await User.create({
      name: "JestBot Admin",
      email: "jestbotai@gmail.com",
      role: "admin",
      password: "password123",
    });
  }

  for (const blog of blogs) {
    await Blog.findOneAndUpdate(
      { slug: blog.slug },
      {
        ...blog,
        author: admin._id,
        publishedAt: new Date(),
      },
      { upsert: true, new: true }
    );
    console.log(`Upserted blog: ${blog.title}`);
  }

  console.log("Done seeding blogs.");
  await mongoose.disconnect();
  process.exit(0);
};

run().catch((err) => {
  console.error(err);
  process.exit(1);
});